/** Replace with your Douyin traffic主 ad unit id before release. */
export const REWARDED_AD_UNIT_ID = 'YOUR_AD_UNIT_ID_HERE';

/** 2x production boost duration after watching rewarded ad (ms). */
export const AD_BOOST_DURATION_MS = 5 * 60 * 1000;

/** Auto production tick interval (seconds). */
export const AUTO_TICK_INTERVAL = 1;

/** Game display name. */
export const GAME_TITLE = '摸鱼工厂';
